name: amazon_cognito_upload
description: The amazon_cognito_upload package is a Dart library designed to simplify and streamline the process of uploading files to Amazon S3 Bucket.
version: 0.0.4
repository: https://github.com/jaiminraval274/amazon_cognito_upload.git

environment:
  sdk: '>=3.1.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  http: ^0.13.5
  amazon_cognito_identity_dart_2: ^3.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

# This package supports all platforms listed below.
platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:
flutter:
